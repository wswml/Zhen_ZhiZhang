<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="微信消息读取器"
        android:icon="@android:mipmap/sym_def_app_icon"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.DeviceDefault.Light">

        <!-- Xposed 模块标识 -->
        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="微信+支付宝双平台账单实时读取，每日自动导出钱迹CSV" />
        <meta-data android:name="xposedminversion" android:value="82" />

        <!-- 每日导出闹钟接收器 (开机+定时) -->
        <receiver android:name=".DailyExportReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.nous.wechatreader.EXPORT_ALARM" />
                <action android:name="com.nous.wechatreader.SCHEDULE_ALARM" />
            </intent-filter>
        </receiver>

        <!-- 支付宝轮询前台服务 (替代失效闹钟，START_STICKY 常驻) -->
        <service
            android:name=".AlipayPollService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="alipay_bill_sync" />
        </service>

        <!-- 启动入口: 点图标启动常驻服务 -->
        <activity
            android:name=".LaunchActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.Dialog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
